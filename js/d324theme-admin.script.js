/**
 * @file
 * Behaviors for the d324theme admin theme.
 */

(function ($, _, Drupal, drupalSettings) {
  'use strict';

  Drupal.behaviors.d324themeAdmin = {
    attach: function (context) {
      // D324theme admin JavaScript behaviors goes here.
    }
  };

})(window.jQuery, window._, window.Drupal, window.drupalSettings);
